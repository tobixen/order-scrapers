* Default target should NOT be ~/regnskap - the silly Norwegian among us wanting to keep the files there should configure it so.  Perhaps ~/order-history/ or ~/accounting/ would be better?  All mentions of `regnskap` in the code and doc should be removed
* Have a chat with the owner of the shopping analyzer project, it would be nice if it was possible to import the code from pypi and add it as a regular dependency (and his dashboard should probably be made multi-shop-aware).
* If this project gets popular, there may be thousands of shops added ... how is it best to scale the project?  Having thousands of `$foo-history` under ~/.local/bin is not an option.  I probably also won't have time doing proper security audit of all the shops added, and I will not be able to test all shops added.
